# #!/bin/bash
#
# # Check if there are saved sessions
# if [ -d ~/.tmux/resurrect ]; then
#     read -p "Do you want to restore the last tmux session? (y/n): " choice
#     case "$choice" in
#         [Yy]* ) tmux resurrect; break;;
#         [Nn]* ) tmux new-session -s new_session; break;;
#         * ) echo "Please answer yes or no.";;
#     esac
# else
#     tmux new-session -s new_session
# fi

#!/bin/bash

# # Check if there are saved sessions
# if [ -d ~/.tmux/resurrect ]; then
#     read -p "Do you want to restore the last tmux session? (y/n): " choice
#     case "$choice" in
#         [Yy]* ) tmux resurrect; break;;
#         [Nn]* ) tmux new-session -s new_session; break;;
#         * ) echo "Please answer yes or no.";;
#     esac
# else
#     tmux new-session -s new_session
# fi

