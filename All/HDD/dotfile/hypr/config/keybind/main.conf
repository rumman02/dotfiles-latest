





$prefix = SUPER



$prefix_only = $prefix, SPACE

$window_sub = [W]indow - [G]ap, [O]pacity, [T]oggleFloat, [F]ullscreen
$prefix_sub = Prefix

# $prefix_only = $prefix, SUPER_L
# $prefix_shift = $prefix&SHIFT
# $prefix_shift_alt = $prefix&SHIFT&ALT
# $prefix_shift_ctrl = $prefix&SHIFT&CTRL
# $prefix_shift_ctrl_alt = $prefix&SHIFT&CTRL&ALT
# $prefix_alt = $prefix&ALT
# $prefix_alt_ctrl = $prefix&ALT&CTRL
# $prefix_ctrl = $prefix&CTRL


source = ~/dotfile/hypr/config/keybind/app.conf
source = ~/dotfile/hypr/config/keybind/mouse.conf
source = ~/dotfile/hypr/config/keybind/system.conf
source = ~/dotfile/hypr/config/keybind/window.conf
source = ~/dotfile/hypr/config/keybind/style.conf
source = ~/dotfile/hypr/config/keybind/workspace.conf

# -----------------------------------------------
# |		[A]apps			|		[B]rowser		|
# -----------------------------------------------
# |						|		[E]xplorer		|
# -----------------------------------------------
# |						|		[T]erminal		|
# -----------------------------------------------
# |						|		[S]earch		|
# -----------------------------------------------
# -----------------------------------------------------------------------------------------------------------------------
# |		[W]indow		|		[B]order		|		[T]hickness		|		[H, J, K, L, R]							|
# -----------------------------------------------------------------------------------------------------------------------
# |						|						|		[R]adius		|		[H, J, K, L, R]							|
# -----------------------------------------------------------------------------------------------------------------------
# |		[W]indow		|		[B]lur			|		[S]ize			|		[H, J, K, L, R]							|
# -----------------------------------------------------------------------------------------------------------------------
# |						|						|		[P]asses		|		[H, J, K, L, R]							|
# -----------------------------------------------------------------------------------------------------------------------
# |		[W]indow		|		[G]ap			|		[I]n			|		[H, J, K, L] [SHIFT]*					|
# -----------------------------------------------------------------------------------------------------------------------
# |						|						|		[O]ut			|		[H, J, K, L] [SHIFT]*					|
# -----------------------------------------------------------------------------------------------------------------------
# |						|						|		[A]all			|		[H, J, K, L] [SHIFT]*					|
# -----------------------------------------------------------------------------------------------------------------------
# |						|						|		[P]asses		|		[H, J, K, L] [SHIFT]*					|
# -----------------------------------------------------------------------------------------------------------------------
# |		[W]indow		|		[M]ove			|						|												|
# -----------------------------------------------------------------------------------------------------------------------
# |						|						|		[O]ut			|		[o]ut					[o]ut			|
# -----------------------------------------------------------------------------------------------------------------------
# |						|						|		[A]all			|		[a]all					[a]all			|
# -----------------------------------------------------------------------------------------------------------------------
# |						|						|		[P]asses		|		[p]asses				[p]asses		|
# -----------------------------------------------------------------------------------------------------------------------
# |		[W]indow		|		[O]opacity		|		[C]urrent		|		[c]urrent				[c]urrent		|
# -----------------------------------------------------------------------------------------------------------------------
# |						|						|		[O]thers		|		[o]thers				[o]thers		|
# -----------------------------------------------------------------------------------------------------------------------
# |						|						|		[F]ullscreen	|		[f]ullscreen			[f]ullscreen	|
# -----------------------------------------------------------------------------------------------------------------------
# |						|						|		[A]all			|		[a]all					[a]all			|
# -----------------------------------------------------------------------------------------------------------------------
# ---------------------------------------------------------------------------
# |		[F12]brightness			|		[1, 2, 3, 4, 5, 6, 7, 8, 9, 0]		|
# ---------------------------------------------------------------------------


# # template
# bind = $prefix_only, submap, prefix
# submap = prefix
# bind = , key, submap, name
# submap = name
# bind = , escape, submap, reset
# bind = $prefix_only, submap, reset
# submap = reset
